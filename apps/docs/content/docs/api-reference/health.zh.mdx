---
title: 健康状态
description: 查看已认证项目的就绪度、持久积压、投影与近期警告。
---

## `GET /v1/health`

```bash
curl https://fishmem.com/v1/health \
  -H "Authorization: Bearer $FISHMEM_API_KEY"
```

需要 `operations:read`。响应区分整体 `healthy`/`degraded`，并报告引擎、任务积压、操作状态、投影与近期警告。它不会调用付费模型，也不会把只可建立 TCP 连接误报为完整就绪。健康读取免费。
